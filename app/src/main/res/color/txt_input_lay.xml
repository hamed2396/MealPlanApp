<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:alpha="1" android:color="#00ACC1" android:state_focused="true" />

    <!-- Unfocused state -->
    <item android:alpha=".6" android:color="@color/mayaBlue" />
</selector>
